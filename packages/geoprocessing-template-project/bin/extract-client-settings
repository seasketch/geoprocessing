#!/usr/bin/env node
const YAML = require("yamljs");
const fs = require("fs");
const path = require("path");

fs.writeFileSync(
  path.join("src/client", "client.json"),
  JSON.stringify(
    YAML.load("serverless.yml").custom.geoprocessing.clients,
    null,
    "  "
  )
);
