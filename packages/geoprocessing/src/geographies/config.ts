const defaultDstPath = "data/dist";

/** Default file location, relative to project root */
const defaultSrcPath = "./project/geographies.json";

export const geographyConfig = {
  defaultSrcPath,
  defaultDstPath,
};
