import Handler from "./area";
import { getExampleSketches } from "@seasketch/geoprocessing";

const calculateArea = Handler.func;

describe("Basic smoke tests", () => {});

describe("Unit tests", () => {});
