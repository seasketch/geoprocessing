version: '3'
services:
  db:
    extends:
      file: ../../gp-workspace/docker-compose.yml
      service: db
    container_name: gp-clip-db
  workspace:
    extends:
      file: ../../gp-workspace/docker-compose.yml
      service: workspace
    volumes:
      - "..:/project"
    container_name: gp-clip-workspace
    depends_on:
      - "db"