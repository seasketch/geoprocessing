#!/usr/bin/env node
const YAML = require("yamljs");
const fs = require("fs");
const path = require("path");

const client = YAML.load("serverless.yml").custom.geoprocessing.client;
fs.writeFileSync(path.join("src/client", "clientConfig.json"));
