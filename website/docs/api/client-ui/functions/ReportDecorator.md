# ReportDecorator()

```ts
function ReportDecorator(storyFn): Element
```

Decorator that renders a story into ReportStoryLayout.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `storyFn` | `any` |

## Returns

`Element`
