# isMetric()

```ts
function isMetric(metric): metric is Object;
```

Checks if object is a Metric and returns narrowed type

## Parameters

| Parameter | Type  |
| --------- | ----- |
| `metric`  | `any` |

## Returns

`metric is Object`
